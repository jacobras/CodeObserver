services:
  app:
    image: mrras/codebase-observer:0.3.0
    container_name: codebase-observer
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/app.db
    restart: unless-stopped